# P2P局域网文件传输工具

推荐一：P2P局域网文件传输工具（带Web界面）
技术点覆盖：
UDP广播/组播实现设备发现（零配置网络）
TCP实现可靠文件传输
自定义应用层协议（文件元数据 + 分片传输 + 断点续传）
多线程/线程池处理并发传输
进度条与流量控制（滑动窗口）
加分项：实现NAT穿透让外网也能用，或加入简单的加密传输。

---

## 一、项目核心功能

| 功能模块 | 说明 |
|---------|------|
| **设备发现** | 新设备加入局域网后自动被其他设备发现，无需手动输入IP |
| **文件传输** | 支持点对点发送文件/文件夹，多文件同时传输 |
| **传输控制** | 暂停/恢复、断点续传、进度显示、速度统计 |
| **连接管理** | 显示在线设备列表、连接状态、最近传输记录 |
| **界面展示** | GUI桌面应用或Web界面，拖拽上传、实时进度条 |

---

## 二、系统架构设计

```
┌─────────────────────────────────────────────────────────┐
│                      应用层 (GUI/Web)                    │
│         (设备列表、文件选择、进度展示、传输历史)              │
├─────────────────────────────────────────────────────────┤
│                      业务逻辑层                          │
│    (文件分片、断点续传、并发控制、任务队列、状态机)          │
├─────────────────────────────────────────────────────────┤
│                      网络通信层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  UDP广播/组播 │  │  TCP文件传输  │  │  信令控制TCP  │  │
│  │  (设备发现)   │  │  (数据通道)   │  │  (请求/响应)  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
├─────────────────────────────────────────────────────────┤
│                      数据层                               │
│         (文件分片缓存、传输记录、配置持久化)                │
└─────────────────────────────────────────────────────────┘
```

---

## 三、详细实现流程（按模块）

### 阶段1：设备发现模块（UDP广播/组播）

**原理**：每台设备周期性向局域网广播自己的"存在信息"，其他设备收到后更新在线列表。

```text
实现步骤：
1. 启动UDP Socket，绑定固定端口（如 8888），设置 SO_BROADCAST 权限
2. 每隔 3 秒发送广播包：{设备名, IP, 服务端口, 唯一ID, 时间戳}
3. 同时监听广播端口，接收其他设备的广播包
4. 维护在线设备列表：收到广播则更新"最后活跃时间"，超时10秒未收到则标记离线
5. 优雅退出时发送"离线通知"，让其他设备立即移除
```

**关键细节**：
- 使用**组播**（Multicast，如 `239.255.255.250`）比广播更规范，避免干扰整个局域网
- 加入简单的**心跳机制**：不仅靠广播，还要单独发送心跳包检测TCP连接是否存活
- 设备唯一ID建议用 MAC地址 + 随机数，避免IP变化导致误判

---

### 阶段2：信令控制通道（TCP短连接）

**作用**：传输正式开始前，双方需要协商"传什么、怎么传"。

```text
自定义应用层协议（建议用JSON + 固定头部）：

请求示例：发送文件协商
{
    "type": "FILE_REQUEST",
    "file_id": "uuid-1234",
    "filename": "论文.pdf",
    "file_size": 10485760,
    "checksum": "md5-hash",
    "chunk_size": 65536,
    "total_chunks": 160
}

响应示例：
{
    "type": "FILE_RESPONSE",
    "status": "ACCEPT",        // ACCEPT / REJECT / PAUSE
    "resume_from_chunk": 0     // 断点续传：从第几个分片开始
}
```

**实现步骤**：
1. 选择目标设备后，向其TCP信令端口（如 8889）建立连接
2. 发送 FILE_REQUEST，等待对方响应
3. 对方弹窗/自动接受后返回 FILE_RESPONSE
4. 若对方选择拒绝，连接关闭；若接受，进入文件传输阶段

---

### 阶段3：文件传输模块（TCP长连接 + 多线程）

**核心设计**：

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  文件读取线程  │ --> │  发送队列    │ --> │  TCP发送线程  │
│  (磁盘I/O)    │     │  (内存缓冲)  │     │  (网络I/O)   │
└─────────────┘     └─────────────┘     └─────────────┘
```

**实现步骤**：

1. **文件分片**：将文件切分为固定大小的块（如 64KB），每块编号
2. **滑动窗口**：发送方维护一个窗口，允许连续发送 N 个块而不等待ACK（类似TCP，但在应用层实现）
3. **接收确认**：接收方收到块后发送 ACK，包含已确认的最大连续块号
4. **超时重传**：发送方超时未收到ACK则重传对应块
5. **写入磁盘**：接收方按顺序写入文件，乱序到达的块暂存内存缓冲区

---

### 阶段4：断点续传与传输控制

**断点续传**：
- 传输中断后，重新发送 FILE_REQUEST，在 `resume_from_chunk` 字段填入已确认的最大块号 + 1
- 接收方检查本地临时文件（`.filename.tmp`），确认已写入的块数
- 只传输缺失的块，避免重复传输

**暂停/恢复**：
- 发送方维护一个 `pause_event`（线程事件），暂停时设置事件，发送线程阻塞
- 恢复时清除事件，继续发送
- 接收方暂停时发送 PAUSE 信令，发送方收到后进入暂停状态

---

### 阶段5：GUI / Web 界面

**方案A：GUI桌面应用（推荐 Python PyQt/Tkinter 或 Electron）**

| 界面元素 | 功能 |
|---------|------|
| 左侧设备列表 | 显示在线设备（头像、设备名、IP），双击发起传输 |
| 中央传输区域 | 拖拽文件到设备图标上即可发送 |
| 底部传输队列 | 实时进度条、速度（MB/s）、预计剩余时间 |
| 右侧历史记录 | 已完成/失败的传输，支持重新发送 |

**方案B：Web界面（推荐 Vue/React + WebSocket）**

- 后端用 Python/Node.js 实现所有网络逻辑
- 前端通过 WebSocket 实时更新设备列表和传输进度
- 优势：跨平台，手机浏览器也能访问
- 劣势：文件下载需要借助浏览器能力，大文件处理稍弱

**界面设计建议**：
- 使用**深色主题** + 霓虹色进度条，演示时投影效果极佳
- 传输时添加**动画效果**（文件图标飞向目标设备）
- 设备发现时添加**通知气泡**（"XXX 加入了网络"）

---

## 四、技术栈推荐

| 层级 | 推荐方案 | 备选方案 |
|-----|---------|---------|
| 语言 | Python（开发快，适合答辩） | C/C++（性能强，难度高） |
| GUI | PyQt6 / PySide6 | Tkinter（简单但丑）、Electron |
| Web | FastAPI + Vue3 | Flask + React |
| 并发 | threading + socket | asyncio（Python 3.7+） |
| 序列化 | JSON（文本可读） | Protocol Buffers（二进制高效） |

**Python 示例依赖**：
```bash
pip install PyQt6  # GUI
pip install aiofiles  # 异步文件I/O
pip install hashlib  # MD5校验（标准库）
```

---

## 五、加分项实现方案

### 加分项1：NAT穿透（让外网设备也能互传）

**原理**：局域网内使用内网IP直连，但外网设备没有公网IP，需要"打洞"。

**实现步骤**：
1. 搭建一个**信令服务器**（有公网IP的最小服务器，只转发握手信息）
2. 设备A和设备B都向信令服务器注册自己的**公网IP:端口**（通过NAT映射）
3. 双方同时向对方发送UDP包（**UDP打洞**），NAT会建立映射关系
4. 一旦打洞成功，双方可以直接通信，无需服务器中转数据

**简化版**：如果无法部署公网服务器，可以演示**STUN协议**（使用Google免费STUN服务器获取自己的公网地址），然后手动交换地址打洞。

---

### 加分项2：加密传输

**方案A：对称加密（AES-256-GCM）**
- 传输前通过信令通道交换密钥（可以用 Diffie-Hellman 密钥交换）
- 每个文件分片单独加密，GCM模式提供认证（防篡改）

**方案B：TLS/SSL**
- 直接用 Python 的 `ssl` 模块包装 TCP Socket
- 自签名证书即可，演示时解释"这是为了模拟HTTPS的安全机制"

---